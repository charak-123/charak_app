name: charak_core
description: Shared package for Charak apps — design tokens, auth, API client, models.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  shared_preferences: ^2.3.3
  http: ^1.2.2
  supabase_flutter: ^2.7.0
  shadcn_ui: ^0.26.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
